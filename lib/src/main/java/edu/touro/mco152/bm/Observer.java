package edu.touro.mco152.bm;

/**
 * Interface for all observers
 */
public interface Observer {
    /**
     * runs whatever code the class that implements Observer wants to run
     */
    void update();
}
